{
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    }
  },
  "DaprSidekick": {
    "Sidecar": {
      // Generic host build doesn't listen on an AppPort - disable here so Sidecar doesn't wait for it indefinitely
      "HasAppPort": false
    }
  }
}

